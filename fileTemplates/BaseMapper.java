#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end
import org.apache.ibatis.annotations.Param;

/**
 * User: ${USER}
 * Date: ${DATE}.
 * Time: ${TIME}
 * ${DESCRIPTION}
 */
public interface ${OBJ}Mapper  extends BaseMapper<${OBJ}> {

}