#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end

/**
 * User: ${USER}
 * Date: ${DATE}.
 * Time: ${TIME}
 * ${DESCRIPTION}
 */
public interface ${OBJ}Service extends TemplateBaseService<${OBJ}>{
 

}